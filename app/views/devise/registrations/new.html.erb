<h2>Sign up</h2>

<%= simple_form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :first_name, required: true, autofocus: true %>
    <%= f.input :last_name, required: true %>
    <%= f.input :role, priority: [ "User" ], collection: [ "User", "Host", "Speaker"], prompt: "Please choose your intended role"%>
    <%= f.input :email, required: true %>
    <%= f.input :password, required: true, hint: ("#{@minimum_password_length} characters minimum" if @minimum_password_length) %>
    <%= f.input :password_confirmation, required: true %>
  </div>

  <div class="form-actions">
    <%= f.button :submit, "Sign up" %>
  </div>
<% end %><br />

<%#= render "devise/shared/links" %>
<%#= image_tag("/assets/google.png", :alt => "home image", :title => "Click here to return to Home") "/" %>
<%=link_to image_tag("/assets/google.png", :width => 250, :height => 80), "/users/auth/google_oauth2"%>
<%=link_to image_tag("/assets/twitter.png", :width => 250, :height => 80), "/users/auth/twitter"%>
